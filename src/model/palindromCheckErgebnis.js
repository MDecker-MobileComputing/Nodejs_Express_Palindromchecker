
export default class PalindromCheckErgebnis {

    constructor( inputNormiert, istPalindrom, laenge, nachricht ) {

        this.inputNormiert = inputNormiert;
        this.istPalindrom  = istPalindrom;
        this.laenge        = laenge;
        this.nachricht     = nachricht;
    }
}
